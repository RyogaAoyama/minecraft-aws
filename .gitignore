# Fabric サーバーのビルド成果物（ランチャー / MOD jar 群）。
# 版は mods.lock / server.lock（どちらも repo 直下・コミット対象）で固定し、
# 実体は CI(build-server.sh --frozen) が都度ダウンロードして S3 へ上げるためコミットしない。
/my-fabric-server/

# ローカル作業ログ
*.log

# MOD jar（CI が都度ダウンロードするためコミットしない）
/mods/

# シークレットを含むローカル専用スクリプト
/sh/create-commands.sh
